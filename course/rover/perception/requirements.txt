# FSK Rover perception (camera streaming, Phase 2) Python dependencies.
opencv-python-headless>=4.9   # USB UVC capture + JPEG encode (no GUI libs)
requests>=2.31                # control SSE + frame POST to the course server
