# Bind-mount source for pilot container's persistent state. Created at
# boot (systemd-tmpfiles) so it exists before pilot.service starts.
# Holds durable calibration files and mission_checkpoint.json.
d /var/lib/pilot 0755 root root -
